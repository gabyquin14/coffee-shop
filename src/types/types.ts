export type Coffee = {
  id: string;
  name: string;
  image: string;
  ingredients: string[];
  description: string;
  price: string;
};
export type CoffeeCart = {
  id: string;
  name: string;
  image: string;
  description: string;
  price: string;
  quantity: number;
};
