import React, { FC } from "react";
import "./shop.css";
import ShopContainer from "./ShopContainer";

export const Shop: FC = () => {
  return (
    <div className="shop">
      <ShopContainer />
    </div>
  );
};

export default Shop;
